{
  "scripts": {
    "build": "postcss src/css/styles.css -o dist/styles.css",
    "watch": "watch 'npm run build' src/"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.1"
  },
  "dependencies": {
    "autoprefixer": "^10.4.14",
    "postcss-cli": "^10.1.0",
    "watch": "^1.0.2"
  }
}
